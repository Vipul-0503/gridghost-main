name: gridghost
description: A Flutter app for theft risk analysis and AR field guidance.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # iOS style icons
  cupertino_icons: ^1.0.6

  # 🌍 Maps (OpenStreetMap)
  flutter_map: ^7.0.2
  latlong2: ^0.9.0

  # 📍 Location
  geolocator: ^9.0.2

  # 🌐 Networking
  http: ^1.2.2

  # 📐 Math utilities
  vector_math: ^2.1.4

  # 📊 Risk gauge
  syncfusion_flutter_gauges: ^32.1.20

  # 📳 Optional feedback
  vibration: ^2.0.1

  # 🔐 Permissions (FIXES Registrar error)
  permission_handler: ^11.3.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/models/
    - assets/images/
